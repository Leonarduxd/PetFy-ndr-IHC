---
trigger: always_on
---

O projeto deve ser construído usando HTML, CSS puro e JavaScript. Todo novo formulário na interface deve ser projetado para se conectar à API backend/MySQL já existente. Não crie lógicas de banco de dados no lado do cliente.